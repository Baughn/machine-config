[package]
name = "nix-build-balancer"
version = "0.1.0"
edition = "2021"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
opt-level = "z"

[dependencies]
rusqlite = { version = "0.32", features = [ "bundled" ] }
