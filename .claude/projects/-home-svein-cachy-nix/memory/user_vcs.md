---
name: Jujutsu VCS preference
description: User prefers Jujutsu (jj) over git for version control operations
type: user
---

Uses Jujutsu (jj) everywhere, including this repo. Finds it easier than git. Use `jj` commands instead of `git` when performing VCS operations.
